name: thumbnailer
version: 0.1.0

targets:
  thumbnailer:
    main: src/thumbnailer.cr

dependencies:
  cli:
    github: devnote-dev/cli.cr
    branch: main
  pluto:
    github: phenopolis/pluto

development_dependencies:
  ameba:
    github: crystal-ameba/ameba
    version: ~> 1.3.1

crystal: 1.6.2

license: ISC
